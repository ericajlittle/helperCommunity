<div class="content">
  <h1 class="room-top title">
    <%= @room.title %>
  </h1>
</div>

<section class="chat">
  <div id="messages"></div>
  <input id='message-input' type='text' data-room-id=<%= @room.id %> data-user-id=<%= current_user.id %> placeholder="You are good to chat!">
</section>
